trigger:
  - master
pr:
  - master

resources:
  repositories:
    - repository: templates
      type: github
      name: ius-gov/pipeline-templates
      endpoint: ius-dev
      ref: 'refs/heads/gd_support_common_chart'

jobs:
  - template: helm-chart-pipeline.yaml@templates
    parameters:
      project: '$(project)'
      helmChartLocation: '.'
      helmChartName: 'common'
